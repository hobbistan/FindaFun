<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:orientation="vertical"
    tools:context="com.findafun.activity.SelectPreferenceActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:background="@android:color/transparent"
        android:gravity="center"
        android:padding="20dp"
        android:text="@string/select_preference"
        android:textColor="@android:color/white"
        android:textSize="19sp"
        android:typeface="serif" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/listView_categories"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="5dp" />

    <TextView
        android:id="@+id/text_getStarted"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/preference_orange"
        android:drawablePadding="10dp"
        android:drawableRight="@drawable/ic_arrow_forward_white_24dp"
        android:gravity="center_vertical|right"
        android:padding="15dp"
        android:text="@string/get_started"
        android:textColor="@android:color/white"
        android:textSize="16sp" />

</LinearLayout>
