<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:background="@drawable/user_background_find_fun"
    android:orientation="vertical">

    <View
        android:id="@+id/view_red_bg"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_alignParentBottom="true"
        android:background="@color/title_red" />

    <com.findafun.customview.CircleImageView
        android:id="@+id/img_profile_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="80dp"
        android:src="@drawable/ic_profile"></com.findafun.customview.CircleImageView>

    <TextView
        android:id="@+id/user_profile_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="25dp"
        android:layout_marginLeft="120dp"
        android:layout_marginTop="10dp"
        android:singleLine="true"
        android:text="Name"
        android:textColor="@android:color/white"
        android:textSize="21dp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/txt_city_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/user_profile_name"
        android:layout_marginBottom="20dp"
        android:layout_marginLeft="120dp"
        android:layout_marginTop="5dp"
        android:text="City"
        android:textColor="@android:color/white"
        android:textSize="18sp" />

</RelativeLayout>